<!--<p id="notice"><%= notice %></p>

<h1>Listing Courses</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Teacher</th>
      <th>Teacher intro</th>
      <th>Description</th>
      <th>Thumbnail url</th>
      <th>Trailer url</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @courses.each do |course| %>
      <tr>
        <td><%= course.title %></td>
        <td><%= course.teacher %></td>
        <td><%= course.teacher_intro %></td>
        <td><%= course.description %></td>
        <td><%= course.thumbnail_url %></td>
        <td><%= course.trailer_url %></td>
        <td><%= link_to 'Show', course %></td>
        <td><%= link_to 'Edit', edit_course_path(course) %></td>
        <td><%= link_to 'Destroy', course, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Course', new_course_path %>-->

<div style="background-color: #fff;">

<div class="container" style="min-height: 875px">
<div class="page-header">

      <div class="span4"> <h1>所有课程   <span class="badge"><%= @courses.count%></span></h1> </div>
      <!--<div class="span4" align="right"><button class="btn"><%= link_to 'New Course', new_course_path %></button></div> -->

  </div>

  
  <div class="row">
      <% @courses.each do |course| %>
    <div class="col-sm-6 col-md-4 col-lg-4">
      <div class="thumbnail"style="height: 100%" >
        <%= image_tag(course.thumbnail_url)%>
        <div class="caption"style="height: 160px"  >
          <h3><%= course.title %></h3>
          <p><%= course.description %></p>
        </div>
        <div class="caption">
         <div align="right"><%= link_to '查看', course,class:"btn btn-default" %> </div>
         </div>
      </div>
    </div>
    <% end %>
  </div>
</div>

</div>
